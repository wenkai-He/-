1.diff算法
    第一:事件缓存  比如说一个按钮绑定了监听事件 onclick会先读取缓存 如果缓存没有就存入缓存 这样就把动态节点变为静态
    第二:静态标记  在会发生变换的地方进行标记 这样下次可以直接找到指定地方进行对比
    第三:静态提升  一些不会更新的元素会被保存起来 下次更新的时候直接复用 这样就可以不需要重复创建节点 优化内存时的连续占用
    第四:比较孩子  头和头比 尾和尾比 然后中间使用最长递增子序列进行比较